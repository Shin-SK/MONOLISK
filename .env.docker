# Django
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=dev-only-change-me
ALLOWED_HOSTS=*

# Postgres（composeのdbサービスを指す）
DATABASE_URL=postgres://monolisk:monolisk@db:5432/monolisk

# CORS/CSRFは既存設定でlocalhost:5173が許可済み

# Mailpit（任意）
EMAIL_HOST=mailpit
EMAIL_PORT=1025
EMAIL_USE_TLS=0
